# Walrus Distributed Message Streaming - Configuration
# Copy this file to .env and customize for your environment

# ============================================================================
# AUTHENTICATION CREDENTIALS
# ============================================================================
# Set strong passwords for production deployment
# If not set, default passwords will be used (NOT RECOMMENDED for production)

# Admin user credentials (full access to all operations)
WALRUS_ADMIN_PASSWORD=admin123

# Producer user credentials (can publish messages only)
WALRUS_PRODUCER_PASSWORD=producer123

# Consumer user credentials (can read messages only)
WALRUS_CONSUMER_PASSWORD=consumer123

# ============================================================================
# USAGE INSTRUCTIONS
# ============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your strong passwords
# 3. Start the server: cargo run
# 4. The server will read passwords from environment variables
#
# For production deployment:
# - Use strong, randomly generated passwords (min 16 characters)
# - Never commit .env file to git (it's in .gitignore)
# - Rotate passwords regularly
# - Use different passwords for each environment (dev/staging/prod)
#
# Alternative: Set environment variables directly
# export WALRUS_ADMIN_PASSWORD="your_secure_password"
# export WALRUS_PRODUCER_PASSWORD="your_secure_password"
# export WALRUS_CONSUMER_PASSWORD="your_secure_password"
# cargo run

# ============================================================================
# SECURITY RECOMMENDATIONS
# ============================================================================
# ✓ Generate strong passwords: pwgen -s 32 1
# ✓ Use different passwords for each role
# ✓ Rotate passwords every 90 days
# ✓ Store API keys securely after first login
# ✓ Never log or print passwords in production
# ✓ Use API keys for service-to-service communication
